meta {
  name: Revoke API Token
  type: http
  seq: 4
}

post {
  url: {{base_url}}/account/api-token/revoke
  body: none
  auth: inherit
}

headers {
  Accept: application/json
}

docs {
  Revokes the current API token.
}
