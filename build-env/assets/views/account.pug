extends _layout.pug

block content
  section.section.is-main-section
    .columns
      .column.is-6.is-offset-3
        .card
          header.card-header
            p.card-header-title
              span.icon
                i.mdi.mdi-account-cog
              | Account Settings
          .card-content
            form(method="post" action="/account")
              .field.is-horizontal
                .field-label.is-normal
                  label.label Web Login Username
                .field-body
                  .field
                    .control.has-icons-left
                      input.input(type="text" name="username" placeholder="Username" value=user)
                      span.icon.is-small.is-left
                        i.mdi.mdi-account
              .field.is-horizontal
                .field-label.is-normal
                  label.label Card Number
                .field-body
                  .field
                    .control.has-icons-left
                      input.input(type="text" name="cardNumber" placeholder="16-character Card Number (optional)" value=fullUser.cardNumber)
                      span.icon.is-small.is-left
                        i.mdi.mdi-credit-card
              .field.is-horizontal
                .field-label.is-normal
                  label.label New Password
                .field-body
                  .field
                    .control.has-icons-left
                      input.input(type="password" name="password" placeholder="Leave blank to keep current")
                      span.icon.is-small.is-left
                        i.mdi.mdi-lock
              .field.is-horizontal
                .field-label.is-normal
                  label.label Confirm Password
                .field-body
                  .field
                    .control.has-icons-left
                      input.input(type="password" name="confirmPassword" placeholder="Confirm new password")
                      span.icon.is-small.is-left
                        i.mdi.mdi-lock-check
              .field.is-grouped.is-grouped-right
                .control
                  if config.discord_client_id
                    if fullUser && fullUser.discordId
                      button.button.is-danger(type="submit" formaction="/account/unlink-discord" onclick="return confirm('Are you sure you want to unlink your Discord account?');" style="margin-right: 10px;")
                        span.icon.is-small
                          i.mdi.mdi-discord
                        span Unlink Discord
                    else
                      a.button.is-link(href="/auth/discord?action=link" style="background-color: #5865F2;")
                        span.icon.is-small
                          i.mdi.mdi-discord
                        span Link Discord
                .control.is-expanded
                  if formMessage
                    if formMessage.danger
                      .notification.is-danger.is-light #{formMessage.message}
                    else
                      .notification.is-success.is-light #{formMessage.message}
                .control
                  button.button.is-link(type="submit")
                    span.icon.is-small
                      i.mdi.mdi-content-save
                    span Save
                    
        if profile
          .card.mt-5
            header.card-header
              p.card-header-title
                span.icon
                  i.mdi.mdi-account-details
                | Profile Details
            .card-content
              form(method="post" action="/account/profile")
                .field.is-horizontal.is-narrow(data-tooltip="Only used in WebUI")
                  .field-label.is-normal
                    label.label Profile Display Name
                  .field-body
                    .field
                      .control.has-icons-left
                        input.input#name-input(type='text' placeholder=profile.name name='name')
                        span.icon.is-small.is-left
                          i.mdi.mdi-account
                .field.is-horizontal.is-narrow
                  .field-label.is-normal
                    label.label PIN
                  .field-body
                    .field
                      .control.has-icons-left
                        input.input(type='password' size='4' maxlength='4' placeholder='••••' name='pin')
                        span.icon.is-small.is-left
                          i.mdi.mdi-dialpad
                .field.is-horizontal.is-narrow(data-tooltip="Set custom PASELI balance (0 - 100000)")
                  .field-label.is-normal
                    label.label Balance
                  .field-body
                    .field
                      .control.has-icons-left
                        input.input#paseli-input(type='number' min='0' max='100000' placeholder=(profile.paseli != null ? profile.paseli : '10000') name='paseli')
                        span.icon.is-small.is-left
                          i.mdi.mdi-currency-usd
                .field.is-grouped.is-grouped-right
                  .control
                    button.button.is-link(type='submit')
                      span.icon.is-small
                        i.mdi.mdi-pencil
                      span Update Profile

        .card.mt-5
          header.card-header
            p.card-header-title
              span.icon
                i.mdi.mdi-key-variant
              | API Token
          .card-content
            p.mb-3 Generate an API token to use with external tools like a Discord bot. One token per account.
            #api-token-status(style="margin-bottom:1.5rem")
              p.has-text-grey Loading...
            #api-token-display(style="display:none;margin-top:1rem")
              .notification.is-warning.is-light
                p
                  strong Copy this token now — it will not be shown again:
                pre#api-token-value(style="word-break:break-all; user-select:all; cursor:pointer")
            .buttons(style="margin-top:1.5rem")
              button#btn-generate-token.button.is-link
                span.icon.is-small
                  i.mdi.mdi-key-plus
                span Generate Token
              button#btn-revoke-token.button.is-danger(style="display:none")
                span.icon.is-small
                  i.mdi.mdi-key-remove
                span Revoke Token

    script.
      (function() {
        var statusEl = document.getElementById('api-token-status');
        var displayEl = document.getElementById('api-token-display');
        var valueEl = document.getElementById('api-token-value');
        var genBtn = document.getElementById('btn-generate-token');
        var revokeBtn = document.getElementById('btn-revoke-token');

        function checkStatus() {
          fetch('/account/api-token/status')
            .then(function(r) { return r.json(); })
            .then(function(d) {
              if (d.exists) {
                statusEl.innerHTML = '<p class="has-text-success"><span class="icon"><i class="mdi mdi-check-circle"></i></span> API token is active.</p>';
                genBtn.style.display = 'none';
                revokeBtn.style.display = '';
              } else {
                statusEl.innerHTML = '<p class="has-text-grey"><span class="icon"><i class="mdi mdi-close-circle"></i></span> No API token set.</p>';
                genBtn.style.display = '';
                revokeBtn.style.display = 'none';
              }
            });
        }
        checkStatus();

        genBtn.addEventListener('click', function() {
          genBtn.classList.add('is-loading');
          fetch('/account/api-token', { method: 'POST', headers: { 'Accept': 'application/json' } })
            .then(function(r) { return r.json(); })
            .then(function(d) {
              genBtn.classList.remove('is-loading');
              if (d.success && d.token) {
                valueEl.textContent = d.token;
                displayEl.style.display = '';
                checkStatus();
              } else {
                alert('Failed to generate token: ' + (d.description || 'Unknown error'));
              }
            });
        });

        revokeBtn.addEventListener('click', function() {
          if (!confirm('Revoke API token? Any services using it will stop working.')) return;
          revokeBtn.classList.add('is-loading');
          fetch('/account/api-token/revoke', { method: 'POST', headers: { 'Accept': 'application/json' } })
            .then(function(r) { return r.json(); })
            .then(function(d) {
              revokeBtn.classList.remove('is-loading');
              displayEl.style.display = 'none';
              checkStatus();
            });
        });
      })();
